# =============================================================================
# nemotron-sheaf — complete dependency list
# =============================================================================
# Core ML
torch>=2.1.0,<3.0.0
transformers>=4.40.0
accelerate>=0.28.0
datasets>=2.18.0

# LoRA fine‑tuning (Phase 0, Phase 2)
peft>=0.17.0
bitsandbytes>=0.43.0          # optional 4‑bit / 8‑bit loading

# Inference validation (Phase 0 Gate 3, Gate 5)
vllm>=0.16.0

# Data generation (Phase 1 – optional back‑ends)
openai>=1.0.0                 # only if using OpenAI API
anthropic>=0.30.0             # only if using Anthropic API
tiktoken>=0.5.0               # precise token counting

# Configuration & contracts
pydantic>=2.0.0
pyyaml>=6.0

# Serialisation
safetensors>=0.4.2

# Mathematics & utilities
numpy>=1.24.0
scipy>=1.10.0                 # used in sheaf loss SVD init, optional

# Experiment tracking (optional)
wandb>=0.16.0

# Development & linting (optional, not imported at runtime)
# black>=24.0.0
# ruff>=0.3.0
# mypy>=1.8.0